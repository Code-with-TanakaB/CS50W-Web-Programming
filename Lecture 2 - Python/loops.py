name = "Alice"

for character in name: #iterating over each character in the string name using a for loop. The loop will execute the block of code inside the loop for each character in the string.
    print(character)